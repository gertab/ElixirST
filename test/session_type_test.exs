defmodule STTest do
  use ExUnit.Case
  doctest ST

  # test "send dual?" do
  #   s1 = "!Hello(any)"
  #   s2 = "?Hello(any)"

  #   session1 = Parser.parse(s1)
  #   session2 = Parser.parse(s2)

  #   assert Duality.dual?(session1, session2) == true
  # end

end
